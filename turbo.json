{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "globalEnv": ["NODE_ENV"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**" , ".dist/**"]
    },
    "lint": {},
    "prebuild" : {
      "cache": false
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
